{
  "name": "deterministic-data-analysis-agent",
  "version": "1.0.0",
  "private": true,
  "description": "PRD v1.0 P0 deterministic data analysis agent",
  "type": "module",
  "bin": {
    "data-agent": "src/cli.js"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "start": "node src/cli.js"
  },
  "engines": {
    "node": ">=20"
  }
}
